gen_header scripts extracts the eui's from the csv and generates a .h file, that will be attached to my firmware / loaded onto a sdcard for on the fly registration

dl_cli has aws cli commands to send dl messages for a particular device