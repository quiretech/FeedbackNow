# Demo / test registry

CSV files here are written when provisioning with `--test`. They are **not** the production master registries in `flexbox_euis/`.

Collision checks include both master registries and this folder when generating new keys.
