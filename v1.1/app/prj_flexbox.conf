# FLEXBOX variant (DEVICE_HW_VARIANT FLEXBOX in onboarding_config.h).
#
# CMake selects this fragment automatically. PN5180, SSD1683, DISPLAY, and LVGL
# are omitted by not adding those Zephyr modules or the flexbox_plus overlay —
# do not reference CONFIG_PN5180 / CONFIG_SSD1683 here (symbols do not exist).
